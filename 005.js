// 005 값(value)와 변수 (variable) 이해하기
var name = "Peter"
var number = 200
var isTrue = true
var nothing = null
var empty = undefined
var list = []
var ref = {}
var func = function () { }

// 동적 바인딩 (Dynamic Binding)
// 프로그래밍 시에 값의 유형을 명시하지 않으면 런타임 시 변수의 값에 의해 동적으로 유형이 결정